export class Stock { 
  constructor (public id: string, public name: string, public wkn: string, public price: string, public branche: string, public index: string, public infoLink: string, public selectedStock: string) {}
}